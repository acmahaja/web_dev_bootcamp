<!-- <!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Campground</title>
</head>

<body>
    <form action="<%= `/campgrounds/${campground.id}/edit?_method=PUT` %> " method="POST">
        <section><label for="title">Title</label><input type="text" name="title" id="title" value="<%= campground.title %> "></section>
        <section><label for="location">Location</label><input type="text" name="location" id="location" value="<%= campground.location %> "></section>
        <button>Update Campground</button>
    </form>
    <a href="<%= `/campgrounds/${campground.id}/`%>">Back to Campground</a>
</body>

</html> -->

<% layout('layouts/boilerplate') %>
    <!-- <form action="<%= `/campgrounds/${campground.id}/edit?_method=PUT` %> " method="POST">
        <section><label for="title">Title</label><input type="text" name="title" id="title"
                value="<%= campground.title %> "></section>
        <section><label for="location">Location</label><input type="text" name="location" id="location"
                value="<%= campground.location %> "></section>
        <button>Update Campground</button>
    </form> -->

    <div class="row">
        <h1 class="text-center">New Campground</h1>
        <div class="col-6 offset-3">
            <form action="<%=`/campgrounds/${campground.id}/edit?_method=PUT`%>" method="POST">
                <div class="mb-3">
                    <label class="form-label" for="title">Title</label>
                    <input class="form-control" type="text" name="title" id="title" value="<%= `${campground.title}` %> ">
                </div>
                <div class="mb-3">
                    <label class="form-label" for="location">Location</label>
                    <input class="form-control" type="text" name="location" id="location" value="<%= `${campground.locatoin}` %> ">
                </div>
                <div class="mb-3">
                    <label class="form-label" for="images">Image URL</label>
                    <input class="form-control" type="url" name="images" id="images" value="<%= `${campground.images}` %> ">
                </div>
    
                <div class="mb-3">
                    <label class="form-label" for="price">Price</label>
                    <div class="input-group">
                        <span class="input-group-text" id="price">$</span>
                        <input type="text" class="form-control" name="price" id="price" placeholder="0.00" value="<%= `${campground.price}` %> "
                            aria-label="price">
                    </div>
                </div>
    
    
                <div class="mb-3">
                    <label class="form-label" for="description">Description</label>
                    <textarea class="form-control" type="url" name="description" id="description" val><%=`${campground.description}`%></textarea>
                </div>
                <div class="mb-3">
                    <button class="btn btn-info">Update Campground</button>
                </div>
            </form>
            <a href="<%= `/campgrounds/${campground.id}/`%>">Back to Campground</a>
        </div>
    </div>
    