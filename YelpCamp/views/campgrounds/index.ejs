<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Campgrounds</title>
</head>
<body>
    <h1>All Campgrounds</h1>
    <a href="/campgrounds/new">New Campground</a>
    <ul>
        <% for(const camp of campgrounds) {%>
            <li><a href="<%= `/campgrounds/${camp.id}` %>"><%= camp.title %></a></li>
        <% } %> 
    </ul>
</body>
</html> -->

<% layout('layouts/boilerplate') %> 
    <h1>All Campgrounds</h1>
    <a href="/campgrounds/new">New Campground</a>
    <!-- <ul>
        <% for(const camp of campgrounds) {%>
            <li><a href="<%= `/campgrounds/${camp.id}` %>">
                    <%= camp.title %>
                </a></li>
            <% } %>
    </ul> -->
    <% for(const camp of campgrounds) {%>
        <div class="card" mb-3>
            <div class="row">
                <div class="col-md-4">
                    <img class="img-fluid" src="<%= camp.images %> " alt="">
                </div>
                <div class="col-md-8">
                    <div class="card-body">
                        <h5 class="card-title"><%= camp.title %> </h5>
                        <p class="card-text"><%= camp.description %> </p>
                        <p>
                            <small class="text-muted"><%= camp.location %> </small>
                        </p>
                        <a href="<%= `/campgrounds/${camp.id}` %>" class="btn btn-primary">View <%= camp.title %> </a>
                    </div>
                </div>
            </div>
        </div>
    <% } %>