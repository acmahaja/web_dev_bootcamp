<%- include('partials/head.ejs')%>

    <body>
        <%- include('partials/navbar.ejs')%>

            <h1>
                Your random number is:
                <%= number%>
                    <% if(number % 2 === 0){ %>
                        <h2>This is an even number</h2>
                        <% } else { %>
                            <h2>This is an odd number</h2>
                            <p>Here is an alternate way of doing it:</p>
                            <%}%>
                                <h3>That number is
                                    <%= number%2===0 ? 'EVEN':'ODD'%>
                                </h3>
            </h1>
    </body>

    </html>